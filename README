This project implements a windows service that invokes a gstreamer pipeline to
record ip cam output (MJPEG), encodes it and dumps into a directory. It kills
the pipeline every N minutes to create a new file. 

USAGE:
=======
run install.bat
